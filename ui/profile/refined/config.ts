export const PROFILE_VISUAL_REFINED = true;

// Consistent Animation Durations
export const MOTION_DURATIONS = {
    FAST: 300,
    MEDIUM: 600,
    SLOW: 1200,
    HALO_LOOP: 16000,
};

export const MOTION_EASING = {
    // Cubic bezier approximations or similar
    // We'll use Reanimated Easing.bezier usually, but defining keys here
    DEFAULT: 'ease-out',
};
